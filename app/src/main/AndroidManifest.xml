<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="fr.cned.emdsgil.suividevosfrais">
  <uses-permission android:name="android.permission.INTERNET">
  </uses-permission>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <application
    android:allowBackup="true"
    android:fullBackupContent="@xml/my_backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".Vue.MainActivity"></activity>
    <activity android:name=".Vue.RepasActivity" />
    <activity android:name=".Vue.EtapesActivity" />
    <activity android:name=".Vue.NuiteeActivity" />
    <activity android:name=".Vue.AuthentificationActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />


        <category android:name="android.intent.category.LAUNCHER" />

      </intent-filter>
    </activity>
    <activity android:name=".Vue.HfActivity" />
    <activity android:name=".Vue.HfRecapActivity" />
    <activity android:name=".Vue.KmActivity" />
  </application>

</manifest>
